Here’s a **clean one-liner summary of all 36 questions**, covering **what you learned** and **the key answer checked** in each.
(Perfect for revision or last-minute exam prep.)

Certification - Python Data Associate https://app.datacamp.com/certification/python-data-associate

### ✅ **36-Question One-Liner Summary**

1. **Filter strings by starting letter (pandas)** — Use `.str.startswith('T')`.
2. **Regex string matching in pandas** — Use `.str.contains('Pro', regex=True)`.
3. **Total missing values in DataFrame** — Use `df.isna().sum().sum()`.
4. **Convert string to datetime** — Use `datetime.strptime()`.
5. **Timezone conversion (pandas index)** — Use `.tz_convert('US/Central')`.
6. **Parse JSON to DataFrame** — Use `pd.DataFrame(data['students'])`.
7. **Correct DataFrame constructor** — Blank is `pd.DataFrame`.
8. **Update API resource** — Use `requests.put()`.
9. **Create API resource** — Use `requests.post()`.
10. **Load SQL table to DataFrame** — Use `pd.read_sql_query()`.
11. **Filter multiple values** — Use `.isin([...])`.
12. **Read pipe-delimited file** — Use `pd.read_csv(sep='|')`.
13. **Filter list with lambda** — Use `filter(lambda x: x >= 15, ...)`.
14. **Multiple conditions in pandas** — Use `&` with parentheses.
15. **Filter emails by domain** — Use `.str.contains('example.com')`.
16. **Execute SQL with SQLAlchemy** — Use `connection.execute()`.
17. **Basic HTML structure** — `<!DOCTYPE> → html → head → title → body`.
18. **Most common value imputation (sklearn)** — Use `strategy='most_frequent'`.
19. **`int("2.3")` behavior** — Raises `ValueError`.
20. **Median imputation (sklearn)** — Use `strategy='median'`.
21. **Grouped boxplot (pandas)** — Use `df.boxplot(by=..., column=...)`.
22. **Constant variance case** — No transformation needed.
23. **Z-score with SciPy** — Use `stats.zscore()`.
24. **Variance (statistics module)** — Use `statistics.variance()`.
25. **Most frequent array value** — Use `stats.mode()`.
26. **Missing data depends on observed vars** — MAR (Missing At Random).
27. **Pearson correlation (NumPy)** — Use `np.corrcoef()[0,1]`.
28. **Spearman correlation (pandas)** — Use `.corr(method='spearman')`.
29. **Mode imputation (sklearn)** — Use `strategy='most_frequent'`.
30. **Seaborn countplot palette** — Use `palette='Blues'`.
31. **Validate Seaborn countplot** — Output confirms correct palette usage.
32. **Linear interpolation (pandas)** — Use `.interpolate()`.
33. **Basic line plot (matplotlib)** — Use `plt.plot(x, y)`.
34. **Multi-line plot (participants)** — Use multiple `plt.plot()` calls.
35. **Skewness (SciPy)** — Use `scipy.stats.skew()`.
36. **Spearman correlation (SciPy)** — Use `scipy.stats.spearmanr()`.
